[project]
name = "signal-trade-engine"
version = "0.1.0"
description = "Binance Futures Testnet trading bot assignment"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-binance>=1.0.19",
]

[project.scripts]
trading-bot = "bot.cli:main"
